class Story {
  constructor (app) {
    //app.addActor();
    //app.addActor();
    //app.addAsset();
  }
  
  start (app) {
    //app.
  }
  
  prePlay (app) {}
  skipPlay () { return false }
  postPlay (app) {}
  
  prePaint (app) {}
  skipPaint () { return false }
  postPaint (app) {}
}

export default Story;